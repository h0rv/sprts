# sprts-core

Provider-neutral score, team, game, league, and date types used by sprts.

Install this directory as a Zig package or add it as a path dependency:

```zig
.sprts_core = .{ .path = "path/to/packages/core" },
```

